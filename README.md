# rainbow-gradient-generator
A simple tool that generates customizable Perlin-noise–based rainbow gradients.
This project lets you create smooth, organic color-shifting textures at any resolution you choose. it can can quickly export the generated gradient as an image file for use in pixel art, shaders, UI elements, VFX, or any project that needs a colorful noise pattern.

I made this tool in like 2 days using Godot to create the "stellar quartz dust" item in my upcoming minecraft modpack

If this tool gets enough views imma make it embeddable and dynamic
